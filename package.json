{
  "name": "clay-screen",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Realtime screen-to-clay transformation with FLUX.2 Klein and an optional Mac fallback.",
  "scripts": {
    "check": "find static -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test tests/*.mjs"
  },
  "dependencies": {
    "@fal-ai/client": "1.10.1"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
