# Required for FLUX.2 cloud mode. Use an API-scoped key; never commit real values.
FAL_KEY=
# Choose a long, private shared secret for people whose usage you will fund.
CLAY_SCREEN_ACCESS_CODE=

# Optional: use the private Apple Silicon fallback instead of browser preview.
# CLAY_SCREEN_BACKEND=mac
